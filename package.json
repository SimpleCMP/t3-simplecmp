{
  "name": "@simplecmp/t3-simplecmp-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright BE tests for the t3_simplecmp extension. Drives the real TYPO3 v14 backend against a ddev instance — login, library browser, detection log, classifier state derivation.",
  "scripts": {
    "test:be": "playwright test",
    "test:be:ui": "playwright test --ui",
    "test:be:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
